# ── Core dependencies ────────────────────────────────────────────
numpy>=1.26
pandas>=2.1
polars>=1.0
pyarrow>=14.0
scipy>=1.12
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
networkx>=3.2

# ── Dimensionality reduction ────────────────────────────────────
umap-learn>=0.5.4
pynndescent>=0.5.11

# ── PyTorch (CPU) — suffisant pour toutes les tâches d'analyse.
#    Pour l'accélération GPU (CUDA), voir le README.
torch>=2.2

# ── Utilities ───────────────────────────────────────────────────
tqdm>=4.66
joblib>=1.3

# ── RAPIDS (optionnel) ──────────────────────────────────────────
# Nécessaire uniquement pour les cellules d'échantillonnage GPU
# (Tâche 0, cellules 8 et 10). NON disponible via pip standard.
# Installer via conda / mamba depuis le canal RAPIDS :
#   conda install -c rapidsai -c conda-forge cudf cupy rmm
# Les données pré-calculées (sample-*/splits/) sont fournies,
# donc RAPIDS n'est PAS requis pour exécuter les tâches 1 à 5.
